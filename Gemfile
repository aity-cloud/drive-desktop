# Only fastlane, and only for one job: fetching the Developer ID certificate
# out of the shared match store before signing the macOS build. The desktop
# client is not an App Store app, so there is no gym/pilot/deliver here.
source "https://rubygems.org"

gem "fastlane"
