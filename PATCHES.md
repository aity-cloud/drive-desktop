# Patch inventory

Hunk-by-hunk inventory of `patches/` (ADR 0001: a patch needs "not
shippable without it"; every patch is re-validated on every Bump).

**Current count: 0.** Everything Aity Drive needs at Pin `v7.1.0` is
expressible through the OEM theme mechanism (`overlay/`): identity,
preset server, OIDC client, colours, icons, updater posture, crash
reporter off. Keep it that way.
